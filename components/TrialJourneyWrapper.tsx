'use client';

import TrialJourney from './TrialJourney';

export default function TrialJourneyWrapper() {
  // Simple client wrapper so the app route can render a client-only component
  return <TrialJourney />;
}
